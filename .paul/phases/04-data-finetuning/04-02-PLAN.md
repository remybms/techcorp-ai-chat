---
phase: 04-data-finetuning
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [notebooks/medical_lora_finetuning.ipynb, reports/data_quality_report.md]
autonomous: true
---

<objective>
## Goal
Créer le notebook Colab de fine-tuning LoRA, le lancer sur Google Colab Pro, et valider les performances conversationnelles du modèle médical entraîné.

## Purpose
La mission expérimentale R&D requiert un modèle médical fine-tuné. Ce modèle ne sera pas déployé en production mais son entraînement est un livrable du hackathon. Colab Pro fournit le GPU nécessaire.

## Output
- `notebooks/medical_lora_finetuning.ipynb` : notebook complet prêt pour Colab Pro
- Modèle fine-tuné sauvegardé dans Colab (Drive ou artefact)
- Résultats de validation dans `reports/data_quality_report.md`
</objective>

<context>
@.paul/PROJECT.md
@.paul/phases/04-data-finetuning/04-01-SUMMARY.md
</context>

<acceptance_criteria>

## AC-1: Notebook LoRA complet et fonctionnel
```gherkin
Given le notebook medical_lora_finetuning.ipynb ouvert dans Colab Pro
When on exécute toutes les cellules séquentiellement
Then le fine-tuning démarre sans erreur d'import ni de configuration
```

## AC-2: Fine-tuning complété
```gherkin
Given le notebook lancé sur GPU Colab Pro
When l'entraînement se termine (ou atteint les epochs définis)
Then la loss d'entraînement a diminué et le modèle est sauvegardé
```

## AC-3: Performances conversationnelles validées
```gherkin
Given le modèle fine-tuné chargé
When on pose 3 questions médicales types
Then le modèle répond avec un contenu médical pertinent (pas de réponse vide ou hors-sujet total)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Créer le notebook Colab de fine-tuning LoRA</name>
  <files>notebooks/medical_lora_finetuning.ipynb</files>
  <action>
    Créer dossier notebooks/.
    Créer notebooks/medical_lora_finetuning.ipynb avec les cellules suivantes :

    Cellule 1 — Install :
    !pip install transformers peft datasets accelerate bitsandbytes trl -q

    Cellule 2 — Imports :
    from transformers import AutoTokenizer, AutoModelForCausalLM, TrainingArguments, BitsAndBytesConfig
    from peft import LoraConfig, get_peft_model, TaskType
    from trl import SFTTrainer
    from datasets import load_dataset
    import torch, json

    Cellule 3 — Config modèle de base :
    BASE_MODEL = "microsoft/phi-3.5-mini-instruct"  # ou phi3.5 selon disponibilité
    Charger avec quantization 4-bit (BitsAndBytesConfig nf4) pour tenir en mémoire Colab

    Cellule 4 — Charger le dataset :
    Uploader data/medical_dataset_clean.json dans Colab (cell avec upload widget ou depuis Drive)
    Charger avec datasets.load_dataset('json', data_files='medical_dataset_clean.json')
    Split train/test : 90%/10%

    Cellule 5 — Config LoRA :
    LoraConfig(r=16, lora_alpha=32, target_modules=["q_proj","v_proj"], lora_dropout=0.05, bias="none", task_type=TaskType.CAUSAL_LM)
    get_peft_model(model, lora_config)
    Afficher nb de paramètres entraînables

    Cellule 6 — Training :
    TrainingArguments : num_train_epochs=3, per_device_train_batch_size=4, learning_rate=2e-4, output_dir="./lora_medical_output"
    SFTTrainer avec dataset_text_field selon format détecté en 04-01
    trainer.train()

    Cellule 7 — Sauvegarde :
    trainer.model.save_pretrained("./lora_medical_output")
    tokenizer.save_pretrained("./lora_medical_output")

    Cellule 8 — Validation :
    Charger le modèle fine-tuné
    Tester 3 questions : "What are the symptoms of diabetes?", "How is hypertension treated?", "What causes anemia?"
    Afficher les réponses

    Cellule 9 — Rapport :
    Afficher : loss finale, nb epochs, nb paramètres entraînés, exemples réponses

    Ajouter des commentaires markdown entre les cellules pour la lisibilité.
    Fixer seed : torch.manual_seed(42)
  </action>
  <verify>Ouvrir notebooks/medical_lora_finetuning.ipynb dans Colab — pas d'erreur de syntaxe, toutes les cellules présentes</verify>
  <done>AC-1 satisfait : notebook complet et importable dans Colab</done>
</task>

<task type="auto">
  <name>Task 2: Documenter les résultats de fine-tuning dans le rapport</name>
  <files>reports/data_quality_report.md</files>
  <action>
    Après avoir lancé le notebook sur Colab Pro (action manuelle de l'utilisateur) :
    Ajouter section "Fine-tuning LoRA" dans reports/data_quality_report.md avec :
    - Modèle de base utilisé
    - Nb entrées d'entraînement / test
    - Config LoRA (r, alpha, modules ciblés)
    - Epochs, learning rate, batch size
    - Loss initiale → loss finale
    - 3 exemples de réponses du modèle fine-tuné
    - Évaluation qualitative : "Le modèle répond de manière médicalement cohérente" (oui/partiel/non)
    - Limites : "Modèle expérimental, ne pas utiliser en production médicale réelle"

    Si le fine-tuning n'est pas encore terminé au moment de rédiger ce rapport, noter les paramètres et compléter après.
  </action>
  <verify>reports/data_quality_report.md contient section "Fine-tuning LoRA" avec les métriques d'entraînement</verify>
  <done>AC-2 et AC-3 satisfaits : fine-tuning documenté et exemples de réponses inclus</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- medical_dataset/ (sources brutes)
- data/medical_dataset_clean.json — ne pas re-nettoyer
- frontend/ — hors scope
- Le modèle fine-tuné NE DOIT PAS être déployé en production

## SCOPE LIMITS
- Ce plan ne déploie pas le modèle médical (expérimental uniquement)
- Ne pas fine-tuner sur le modèle Phi-3.5-Financial (garder les deux séparés)
- Si Colab plante par manque de RAM : réduire batch_size=2 et r=8

</boundaries>

<verification>
- [ ] notebooks/medical_lora_finetuning.ipynb créé et valide
- [ ] Toutes les cellules exécutables séquentiellement
- [ ] Seed fixée à 42
- [ ] Section "Fine-tuning LoRA" dans reports/data_quality_report.md
- [ ] AC-1, AC-2, AC-3 satisfaits
</verification>

<success_criteria>
- Notebook LoRA prêt pour Colab Pro
- Fine-tuning complété (loss diminuée)
- 3 questions médicales reçoivent des réponses cohérentes
- Rapport documenté
</success_criteria>

<output>
Après completion, créer `.paul/phases/04-data-finetuning/04-02-SUMMARY.md`
</output>
